from flask import session

from datetime import timedelta
app.permanent_session_lifetime = timedelta(minutes = 15)
#timedelta(minutes = 15) means that the session will last 15 minutes before closing on its own

#you can add information to sessions using (session[{variable name}] = {information})
#e.g. session["user"] = user
